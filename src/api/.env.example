# Secrets live in the repo-root `.env`. Copy from ../../.env.example.
# This file is only a reminder of the mail keys the API reads.

SECRET_SMTP_HOST=
SECRET_SMTP_PORT=
SECRET_SMTP_USER=
SECRET_SMTP_PASS=
SECRET_SMTP_FROM=
SECRET_CONTACT_TO=
SECRET_GEOAPIFY=
